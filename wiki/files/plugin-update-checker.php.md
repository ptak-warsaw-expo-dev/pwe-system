# `plugin-update-checker.php`

Plik first-party PWE System w kategorii `updater-wrapper`.

## Metadane

- **Kategoria:** `updater-wrapper`
- **Rozmiar:** 306 B
- **Liczba linii:** 11
- **Źródło:** `plugin-update-checker.php`

## Typy i metody

- Brak klas/traitów.

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych/reprodukowanych rejestracji.

## Wybrane wywołania statyczne

- Brak.

## API WordPress / GF rozpoznane heurystycznie

- Brak.

## Dołączane pliki / wyrażenia include

- `_once __DIR__ . '/plugin-update-checker/load-v4p9.php'`

## Tabele / źródła SQL

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole są wyciągane przez `token_get_all`; traity są zachowane z `kind=trait`.
- Dynamiczne rejestracje AJAX z map/stałych są rozwinięte w `hooks.json` i `entrypoints.json`.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
