# `modules/forms-audit/traits/trait-pwe-forms-audit-export.php`

Plik first-party PWE System w kategorii `forms-audit`.

## Metadane

- **Kategoria:** `forms-audit`
- **Rozmiar:** 5880 B
- **Liczba linii:** 172
- **Źródło:** `modules/forms-audit/traits/trait-pwe-forms-audit-export.php`

## Typy i metody

### trait `PWE_System_Forms_Audit_Export_Trait` — linia 7

  - `public export_mismatches_csv()` — linia 9

## Funkcje globalne

- Brak funkcji globalnych.

## Rejestracje WordPress / GF

- Brak wykrytych/reprodukowanych rejestracji.

## Wybrane wywołania statyczne

- `GFAPI::get_forms()`
- `GFAPI::get_entries()`

## API WordPress / GF rozpoznane heurystycznie

- `current_user_can()`
- `wp_die()`
- `check_admin_referer()`
- `sanitize_text_field()`
- `wp_unslash()`
- `wp_parse_url()`
- `home_url()`
- `wp_date()`
- `is_wp_error()`

## Dołączane pliki / wyrażenia include

- Brak.

## Tabele / źródła SQL

- Brak statycznie rozpoznanych.

## Uwagi

- Symbole są wyciągane przez `token_get_all`; traity są zachowane z `kind=trait`.
- Dynamiczne rejestracje AJAX z map/stałych są rozwinięte w `hooks.json` i `entrypoints.json`.
- Dokładny Call Graph powstaje niezależnie w Code Indexerze.
